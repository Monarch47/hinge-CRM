# Node
node_modules/
npm-debug.log*
.env
# Captured device data — NEVER commit real people's profile data.
# Screenshots and UI dumps are transient working files only.
*.png
!docs/*.png
ui*.xml
dump_*.xml
prof_*.png
hinge_*.png
/captures/
/scratch/
/screenshots/
/screenshots_cropped/
collage_*.jpg
# Private local CRM log. Never commit, never git-rm, never force-add.
hinge-log.html

# Local, private outreach data (should never be shared)
hinge-log.data.json
# Debug run logs — contain scraped profile text and full UI dumps.
/logs/

# OS
.DS_Store
.cursor/